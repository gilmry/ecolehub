# EcoleHub - Configuration générique
# Copier ce fichier vers .env et modifier les valeurs selon votre stage et environnement

# ==========================================
# CONFIGURATION DE BASE
# ==========================================

# Stage de déploiement (0, 1, 2, 3, ou 4)
STAGE=4

# Domaine principal pour votre instance
DOMAIN=localhost
# DOMAIN=votre-ecole.example.com

# Email pour les certificats Let's Encrypt (production uniquement)
LETSENCRYPT_EMAIL=admin@example.com

# CORS Origins (séparés par des virgules)
CORS_ORIGINS=http://localhost,https://localhost

# ==========================================
# SÉCURITÉ - OBLIGATOIRE À CHANGER EN PRODUCTION
# ==========================================

SECRET_KEY=change-this-in-production-very-important-long-secret-key

# Mots de passe des services (changez en production)
DB_PASSWORD=ecolehub_secure_password
REDIS_PASSWORD=ecolehub_redis_cache_password
GRAFANA_PASSWORD=ecolehub_grafana_admin

# MinIO (stockage fichiers)
MINIO_ACCESS_KEY=ecolehub_minio_admin
MINIO_SECRET_KEY=ecolehub_minio_secure_password

# ==========================================
# APIS EXTERNES (optionnel selon le stage)
# ==========================================

# Mollie (paiements - Stage 3+)
MOLLIE_API_KEY=test_mollie_demo_key

# Printful (impression - Stage 3+)
PRINTFUL_API_KEY=test_printful_demo_key

# ==========================================
# BASE DE DONNÉES (selon le stage)
# ==========================================

# Stage 0: SQLite
# DATABASE_URL=sqlite:///./db/ecolehub.db

# Stage 1+: PostgreSQL
DATABASE_URL=postgresql://ecolehub:${DB_PASSWORD}@postgres:5432/ecolehub

# ==========================================
# REDIS (Stage 2+)
# ==========================================

REDIS_URL=redis://:${REDIS_PASSWORD}@redis:6379/0

# ==========================================
# MONITORING (Stage 4)
# ==========================================

PROMETHEUS_ENABLED=true